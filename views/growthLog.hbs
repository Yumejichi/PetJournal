<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" type="text/css" href="/styles/newPost.css">
</head>
<body>
    <div id="divMain">
        <h1>{{title}}</h1>
        <ul>
            {{#each pet.growthLog}}
            <li>
                <strong>Date:</strong> {{this.measureDate}} <br />
                <strong>Weight:</strong> {{this.weight}} lbs<br />
                <strong>Height:</strong> {{this.height}} ft<br />
                <strong>Note:</strong> {{this.note}}<br />
                <form action="/pets/{{../pet._id}}/growthLog/{{this._id}}/delete" method="post" style="display:inline;">
                    <button type="submit">Delete</button>
                </form>
            </li>
            {{/each}}
        </ul>

        <h3>Add New Growth Log</h3>
        <form action="/pets/{{pet._id}}/growthLog/add" method="post">
            <div>
                <label for="weight">Weight (lbs):</label>
                <input type="number" id="weight" name="weight" step="0.01" required>
            </div>
            
            <div>
                <label for="height">Height (ft):</label>
                <input type="number" id="height" name="height" step="0.01" required>
            </div>

            <div>
                <label for="note">Note:</label>
                <textarea id="note" name="note"></textarea>
            </div>
            
            <div>
                <label for="measureDate">Measurement Date:</label>
                <input type="date" id="measureDate" name="measureDate" required>
            </div>

            <button type="submit">Add Growth Log</button>
        </form>

        <a href="/profile">Back to Profile</a>
    </div>
</body>
</html>
