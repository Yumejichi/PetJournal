<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" type="text/css" href="/styles/style.css">
</head>
<body>
    <h1>{{title}}</h1>
    <ul>
        {{#each pet.growthLog}}
        <li>
            Date: {{this.measureDate}} <br />
            Weight: {{this.weight}} lbs<br />
            Height: {{this.height}} ft<br />
            Note: {{this.note}}<br />
            <form action="/pets/{{../pet._id}}/growthLog/{{this._id}}/delete" method="post" style="display:inline;">
                <button type="submit" style="background-color: #ff4d4d; color: white; border: none; padding: 5px 10px; border-radius: 4px; cursor: pointer;">Delete</button>
            </form>
        </li>
        {{/each}}
    </ul>

    <h3>Add New Growth Log</h3>
    <form action="/pets/{{pet._id}}/growthLog/add" method="post">
        <label for="weight">Weight (lbs):</label>
        <input type="number" id="weight" name="weight" step="0.01" required><br />
        
        <label for="height">Height (ft):</label>
        <input type="number" id="height" name="height" step="0.01" required><br />

        <label for="note">Note:</label>
        <textarea id="note" name="note"></textarea><br />
        
        <label for="measureDate">Measurement Date:</label>
        <input type="date" id="measureDate" name="measureDate" required>
        <button type="submit">Add Growth Log</button>
    </form>

    <a href="/profile">Back to Profile</a>
</body>
</html>
